use crate::benchmark::{HttpLoadConfig, ScenarioConfig, SensorScenarioConfig, Thresholds};

/// Built-in benchmark scenarios used when the TOML file lists no `scenarios`.
///
/// # Returns
///
/// A `Vec` covering baseline, data-loss, HTTP stress, and scalability profiles.
pub fn default_scenarios() -> Vec<ScenarioConfig> {
    vec![
        ScenarioConfig {
            id: "baseline".to_string(),
            description: "Functional baseline with light HTTP load".to_string(),
            warmup_secs: 20,
            measure_secs: 120,
            cooldown_secs: 10,
            buffer_capacity: 5000,
            sensors: SensorScenarioConfig {
                thermo_count: 2,
                thermo_rates_per_sec: "50 30".to_string(),
                accel_count: 2,
                accel_rates_per_sec: "50 50".to_string(),
                force_count: 1,
                force_rates_per_sec: "50".to_string(),
            },
            http_load: Some(HttpLoadConfig {
                enabled: true,
                endpoint: "/api/latest".to_string(),
                endpoints: vec![
                    "/api/latest".to_string(),
                    "/api/stats".to_string(),
                    "/api/sensor/thermo-1".to_string(),
                ],
                concurrency: 1,
                timeout_ms: 1000,
            }),
            thresholds: Thresholds::default(),
        },
        ScenarioConfig {
            id: "data_loss_normal".to_string(),
            description: "Data loss verification under normal sustained load".to_string(),
            warmup_secs: 20,
            measure_secs: 180,
            cooldown_secs: 10,
            buffer_capacity: 6000,
            sensors: SensorScenarioConfig {
                thermo_count: 4,
                thermo_rates_per_sec: "20 20 20 20".to_string(),
                accel_count: 4,
                accel_rates_per_sec: "20 20 20 20".to_string(),
                force_count: 4,
                force_rates_per_sec: "20 20 20 20".to_string(),
            },
            http_load: Some(HttpLoadConfig {
                enabled: true,
                endpoint: "/api/stats".to_string(),
                endpoints: vec![],
                concurrency: 10,
                timeout_ms: 1000,
            }),
            thresholds: Thresholds::default(),
        },
        ScenarioConfig {
            id: "perf_http_100".to_string(),
            description: "Performance with high HTTP concurrency".to_string(),
            warmup_secs: 20,
            measure_secs: 180,
            cooldown_secs: 10,
            buffer_capacity: 6000,
            sensors: SensorScenarioConfig {
                thermo_count: 2,
                thermo_rates_per_sec: "60 60".to_string(),
                accel_count: 2,
                accel_rates_per_sec: "60 60".to_string(),
                force_count: 2,
                force_rates_per_sec: "60 60".to_string(),
            },
            http_load: Some(HttpLoadConfig {
                enabled: true,
                endpoint: "/api/latest".to_string(),
                endpoints: vec![],
                concurrency: 100,
                timeout_ms: 1200,
            }),
            thresholds: Thresholds::default(),
        },
        ScenarioConfig {
            id: "scalability_12_sensors".to_string(),
            description: "Scalability with 12 sensors at fixed rates".to_string(),
            warmup_secs: 20,
            measure_secs: 180,
            cooldown_secs: 10,
            buffer_capacity: 9000,
            sensors: SensorScenarioConfig {
                thermo_count: 4,
                thermo_rates_per_sec: "50 50 50 50".to_string(),
                accel_count: 4,
                accel_rates_per_sec: "50 50 50 50".to_string(),
                force_count: 4,
                force_rates_per_sec: "50 50 50 50".to_string(),
            },
            http_load: Some(HttpLoadConfig {
                enabled: true,
                endpoint: "/api/stats".to_string(),
                endpoints: vec![],
                concurrency: 20,
                timeout_ms: 1000,
            }),
            thresholds: Thresholds::default(),
        },
        ScenarioConfig {
            id: "scalability_18_sensors".to_string(),
            description: "Scalability with 18 sensors at fixed rates".to_string(),
            warmup_secs: 20,
            measure_secs: 180,
            cooldown_secs: 10,
            buffer_capacity: 12000,
            sensors: SensorScenarioConfig {
                thermo_count: 6,
                thermo_rates_per_sec: "45 45 45 45 45 45".to_string(),
                accel_count: 6,
                accel_rates_per_sec: "45 45 45 45 45 45".to_string(),
                force_count: 6,
                force_rates_per_sec: "45 45 45 45 45 45".to_string(),
            },
            http_load: Some(HttpLoadConfig {
                enabled: true,
                endpoint: "/api/stats".to_string(),
                endpoints: vec![],
                concurrency: 24,
                timeout_ms: 1000,
            }),
            thresholds: Thresholds::default(),
        },
        ScenarioConfig {
            id: "scalability_24_sensors".to_string(),
            description: "Scalability with 24 sensors at fixed rates".to_string(),
            warmup_secs: 25,
            measure_secs: 180,
            cooldown_secs: 10,
            buffer_capacity: 16000,
            sensors: SensorScenarioConfig {
                thermo_count: 8,
                thermo_rates_per_sec: "40 40 40 40 40 40 40 40".to_string(),
                accel_count: 8,
                accel_rates_per_sec: "40 40 40 40 40 40 40 40".to_string(),
                force_count: 8,
                force_rates_per_sec: "40 40 40 40 40 40 40 40".to_string(),
            },
            http_load: Some(HttpLoadConfig {
                enabled: true,
                endpoint: "/api/stats".to_string(),
                endpoints: vec![],
                concurrency: 32,
                timeout_ms: 1200,
            }),
            thresholds: Thresholds::default(),
        },
        ScenarioConfig {
            id: "stress_mixed".to_string(),
            description: "Mixed stress with high ingest and HTTP concurrency".to_string(),
            warmup_secs: 20,
            measure_secs: 300,
            cooldown_secs: 20,
            buffer_capacity: 12000,
            sensors: SensorScenarioConfig {
                thermo_count: 4,
                thermo_rates_per_sec: "120 120 120 120".to_string(),
                accel_count: 4,
                accel_rates_per_sec: "120 120 120 120".to_string(),
                force_count: 4,
                force_rates_per_sec: "120 120 120 120".to_string(),
            },
            http_load: Some(HttpLoadConfig {
                enabled: true,
                endpoint: "/api/latest".to_string(),
                endpoints: vec![],
                concurrency: 200,
                timeout_ms: 1500,
            }),
            thresholds: Thresholds::default(),
        },
    ]
}
